# requirements.txt – runtime dependencies
# CLI metadata is used to generate a checked-in reference; keep its renderer stable.
typer==0.27.2
click==8.5.0
rich==15.0.0
openai==3.1.0
python-dotenv==1.2.3
tenacity==9.1.4
